spring:
  application:
    name: throttle
  data:
    redis:
      host: localhost
      port: 6379

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

server:
  port: 8080

ratelimit:
  capacity: 10
  refill-rate: 5.0
  path-prefix: /api